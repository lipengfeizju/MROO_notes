I'm currently considering conducting an experiment for online optimization problem, such as scheduling decisions or controlling problems with a sequentially revealed context. Suppose x_t is the action for time step t.  In each time step t, there is a performance metric. For instance, we define the cost as m/2*(x_t - y_t)^2, m is a constant number, and y_t is the configuration for the optimal solution, and the cost goes up quadratically w.r.t the difference between the action and the per step optimal configuration. In the mean time, we also have a switching cost, b/2*(x_t - x_{t-1})^2, penalizing the action change. For instance, it could be the waiting time for starting a new machine. But again, we want the function to be quadratic. For the switching cost, different application have different impacts. 

For application $i \in [N]$, we define the factor as c_i, so the memory cost for the application is d_{i,t} = c_i*b/2*(x_t - x_{t-1})^2. And d_{t} = [d_{i,t}, ..., d_{N,t}] we want to make sure such impact is equalized in the long run. For instance, we can minimize q(\sum{t=1}^T d_t) and q as the L infinity norm. To sum up, the cost objective is defined as
{cost}(u_{1:T})   =  \frac{1}{T}\sum_{t=1}^T f_t(u_t)  + q\Bigl(\frac{1}{T}\sum_{t=1}^T d_t \bigl(u_t, x_{t-1}\bigr)
\Bigr).

So based on the these papers, do you think we can somehow make the current experiment grounded with the paper's context? I'm trying to make a compelling story from these context. If so, help me design the formulation and set the necessary configuration for this. Also, help me decide how to get the context $y_t$ from public dataset.